# The prototype itself needs none of this: clausola.html opens with a double
# click and the deterministic scripts run on the standard library alone.
#
# These two are needed only for the extraction step — extractor.py and,
# through it, evaluate.py.

litellm      # one interface to LM Studio, to a hosted endpoint, or to our own GPU
pymupdf      # reading a contract that arrives as a PDF rather than as text
